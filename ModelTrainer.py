def trainModel(statsNames):
    # each item in the list statsNames is a valid path to a file
    # which is the captured stats for training the model
    a = 0
    # just train the model. no need to return anything